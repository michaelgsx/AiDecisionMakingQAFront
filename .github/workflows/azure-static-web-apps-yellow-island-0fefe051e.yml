# Disabled: use deploy-qa-swa.yml (Key Vault token) instead.
# Azure Portal auto-generated this workflow; keeping the file avoids re-creation on link,
# but it no longer runs on push/PR.

name: Azure Static Web Apps CI/CD (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use workflow Deploy QA frontend (ai-rag-agentic-qa) instead."
